[build]
  command = "npm run build"
  publish = "dist"

# SPA: all routes serve index.html (optional, useful if you add routing later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
